## 🤖 AI Change Summary for PR #27

### 개요
이번 변경 사항은 Flutter 애플리케이션의 UI 구성 요소를 추가하고 수정하는 내용을 포함합니다. 여러 파일이 제거되고 새로운 위젯이 추가되었으며, 스토리 편집기 화면과 관련된 기능이 강화되었습니다. 또한, URL 입력 및 배경 음악 설정 기능이 추가되었습니다.

### 주요 변경 사항
1. **파일 추가**
   - `story_preview_area.dart`: 스토리 미리보기 영역을 위한 새로운 위젯 추가.
   - `story_tools_panel.dart`: 스토리 편집 도구 패널을 위한 새로운 StatefulWidget 추가.
   - `StoryEditorScreen`: 스토리 편집기 화면을 위한 새로운 StatefulWidget 클래스 추가.
   - `DecorateTabContent`: 스티커 추가 및 카테고리 선택 기능을 제공하는 StatefulWidget 클래스 추가.
   - `MoodTabContent`: 배경 색상 선택을 위한 StatelessWidget 클래스 추가.
   - `SoundTabContent`: YouTube URL 입력 및 배경 음악 설정 UI를 제공하는 새로운 위젯 추가.

2. **파일 제거**
   - `date_selector.dart`: 날짜 선택 기능을 제공하는 위젯 제거.
   - `type_button.dart`: 타입 선택 버튼을 제공하는 위젯 제거.
   - `editor_screen.dart`: 편집 화면을 구성하는 주요 위젯 제거.

3. **기능 변경**
   - `StoryToolsPanel`에서 날짜 선택 기능이 제거되고, 다양한 편집 기능을 제공하는 새로운 도구 패널이 추가됨.
   - `DateGriditem` 클래스의 텍스트 색상이 `onBackground`에서 `onSurface`로 변경됨.

4. **패키지 추가**
   - `pubspec.lock` 파일에 `dotted_border` 패키지가 추가됨.

### 위험/영향
- **기능 손실**: 날짜 선택 기능이 제거되어 사용자가 날짜를 선택할 수 없게 됨.
- **UI 변경**: 새로운 UI 구성 요소가 추가되어 기존 사용자 경험에 변화가 생길 수 있음.
- **상태 관리**: 여러 상태를 관리해야 하므로, 상태 관리 로직이 복잡해질 수 있음.
- **URL 검증 미구현**: `SoundTabContent`의 URL 검증 로직이 아직 구현되지 않아 기능적으로 완전하지 않음.

### 테스트/검증
- 새로운 위젯들에 대한 단위 테스트 및 통합 테스트가 필요함.
- UI 테스트를 통해 새로운 UI 요소들이 올바르게 렌더링되는지 확인해야 함.
- `DateGriditem`의 색상 변경이 의도한 대로 반영되었는지 확인하기 위한 시각적 테스트가 필요함.
- 추가된 패키지가 정상적으로 작동하는지 확인하기 위한 통합 테스트 필요.

### 후속 조치
- 제거된 기능의 대체 방안을 마련하고, 사용자 피드백을 통해 필요한 기능을 재구성할 필요가 있음.
- `SoundTabContent`의 URL 검증 로직을 구현하여 기능을 완전하게 할 필요가 있음.
- 새로운 UI 구성 요소에 대한 사용자 테스트를 진행하여 사용자의 반응을 확인하고 개선점을 도출해야 함.
- 새로운 패키지의 사용 목적과 필요성을 검토하고, 불필요한 의존성이 발생하지 않도록 관리해야 함.
